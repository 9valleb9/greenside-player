# Greenside Player Configuration
# Copy to .env and update values as needed

# API base URL — edge device (local) or cloud
API_BASE=http://10.1.10.205:3000

# Player mode: kiosk (no controls) or web (minimal controls)
MODE=kiosk

# Screen rotation for portrait TVs: 0, 90, 180, 270
ROTATION=0
